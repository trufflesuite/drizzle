{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "prepare": "lerna run prepare",
    "verdaccio:bounce": "./scripts/registry-bounce.sh",
    "verdaccio:kill": "./scripts/registry-kill.sh"

  },
  "devDependencies": {
    "lerna": "^3.15.0"
  }
}
